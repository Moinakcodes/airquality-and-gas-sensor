#include <WiFi.h>
#include <esp_now.h>

#define MQ6_PIN 34

typedef struct struct_message {
  int gasValue;
} struct_message;

struct_message msg;

uint8_t broadcastAddress[] = {0x24, 0x6F, 0x28, 0xAB, 0xCD, 0xEF};


